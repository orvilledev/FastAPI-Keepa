import VendorSchedulerCountdown from './VendorSchedulerCountdown'

export default function REFSchedulerCountdown() {
  return (
    <VendorSchedulerCountdown vendor="ref" title="REF Keepa Off Price Daily Run" />
  )
}
